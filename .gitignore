# Ignore every repository-root entry by default. Only the release site,
# its validation tooling, and essential repository metadata are allowlisted.
/*

!/.gitignore
!/README.md
!/DESIGN.md
!/docs/
!/scripts/
!/tests/

# Keep generated planning material and macOS metadata out of the release tree.
/docs/superpowers/
.DS_Store
.omx/
